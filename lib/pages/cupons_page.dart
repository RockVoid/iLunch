import 'package:flutter/material.dart';

class CuponsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        body: Center(child: Text("Cupons",style: TextStyle(fontSize: 60),)),
      );
}
